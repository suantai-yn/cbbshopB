package com.example.cbbshop.utils;

import com.example.cbbshop.model.Order;

public class OrderValidator {
   /* public static boolean validateOrder(Order order) {
        if (order.getBuyerId() == null || order.getProductId() == null) {
            return false;
        }

        if (order.getProductPrice() <= 0 || order.getTotalPrice() <= 0) {
            return false;
        }

        // 更多校验规则可以根据需求来补充
        return true;
    }

    */
}
